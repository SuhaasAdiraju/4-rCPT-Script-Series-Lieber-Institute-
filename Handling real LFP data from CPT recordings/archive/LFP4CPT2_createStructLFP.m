%% Description
    % This script is a template for the initial process of handling CPT recorded LFP
    % data using the createStructLFP function
        % More specifically how to combine the event data with recordings 
    

    % CONTENTS OF THIS SCRIPT HAS BEEN MOVED WITHIN THE FUNCTION, FOR
    % EASE AND CLARITY
        %createStructLFP(lfp_path,lfp_name, mousename, saveplace, Tstamps_path, Tstamps_filename);
                   
end

waitfor(msgbox(sprintf('Okay, this script is finished, moving on to the next option in the pipeline')))
